# 生产前置核查计划（上线前必须过的清单）

状态：⏳待做（清单已建，未逐项核查）
任务：dev-20260914-008（任务 1757）
被取代 / 取代：无
什么情况看这篇：准备上生产、或判断"现在能不能上"时，从这一页开始
最后复核：2026-09-14

> 分层口径：🔴 **硬前置**＝不做就上不了（或上了会出事）／🟡 **要拍板**＝你定完才能排期或执行／🟢 上线当天或之后可做。
> 用法：每查完一项，把命令 + 输出填进文末「核查进度表」，状态改 ✅；没证据不算查过。

## 一、数据库（最要紧）

| # | 层 | 项 | 现状（2026-09-14 实测） | 要核查 / 补齐什么 |
|---|---|---|---|---|
| 1 | 🔴 | 版本记账断链 | `ops.schema.applied` = 66…101,**107,108,109**（version=109），**102~106 不在集合里**，但 102（DROP production_tooling / jjx_screen_master）、103（建 5 张工程资源表）明显已执行 | 逐号确认 102~106 是否真执行过（看表在不在）；确认后 `bash scripts/db-migrate.sh --record <NN> --yes` 补记录 |
| 2 | 🔴 | DDL 基座缺失 | 迁移目录现存 3 个文件（107/108/109），**含 CREATE TABLE 的 = 0 个**；101~106 在工作区被删（git 历史 `7643353f` 里还有）；全库 **111 张表**的建表语句散在历史迁移里 | 定"新环境怎么建库"：A 用全库 dump 恢复（git 里 1908 那份已过时，缺 107~109 之后的结构）；B 生成一份 DDL 基线（`mysqldump --no-data`）+ 后续迁移补齐 |
| 3 | 🟡 | 迁移 101~106 的归属 | 工作区已删、未提交；删了以后"已执行却没有源文件"，别人 clone 拿不到 | 三选一：恢复 / 提交删除（`--no-verify`）/ 移到仓库外备份目录留档 |
| 4 | 🟡 | 迁移 107~109 未提交 | `??` 未跟踪，但库已应用并记账 | 提交（否则下一个人不知道库里已应用） |
| 5 | 🔴 | 生产库的连接与断言 | 脚本/代码里的库名断言、账号、备份目录全是开发口径：库名 `jjx_erp_db`、`root/123456`、`JJX_BACKUP_DIR` 默认仓库同级 | 生产换：库名/账号/强口令、备份目录指向生产外部存储、脚本里的目标库断言核对 |

## 二、数据初始化

| # | 层 | 项 | 现状 | 要核查 / 补齐什么 |
|---|---|---|---|---|
| 6 | 🔴 | 子集快照是否最新 | 今天 `--verify` = ✓ 最新（19 表 / 7336 行），但那是 107~109 执行**之前**的数据 | 107~109 之后重跑 `--verify`，必要时重出快照 |
| 7 | 🟡 | DECISIONS 5 个待定项 | 静态文件（附件/图纸/打印模板）怎么交付；目标库是"空库全量"还是"仅数据"；ID 是否原样保留；测试脏数据是否先清；`quality_template_registry.file_id` 是否置空 | 逐条拍板（拍板后写进 `sql/init/DECISIONS.md`） |
| 8 | 🟡 | 已知数据缺口 | `inventory_item` 缺 66 条（1601 物料 vs 1535 身份）、`item_code` 与物料现编码不一致、`sys_attachment` 记录与磁盘文件不一致（悬挂引用）、`sys_attachment` 现 1 行 | 定"迁移后对齐"由谁做、什么时候做 |
| 9 | 🔴 | 测试数据不得进生产 | dev 库里有：`sys_task` 看板 800+ 条、测试单据、OCR 测试档案与样本等 | 生产初始化**用子集快照 + DDL**，不是"清测试数据"；`scripts/db-clean-test-data.sh` 只对 dev 库名断言，**不可对生产库使用** |

## 三、敏感信息 / 脱敏

| # | 层 | 项 | 现状 | 要核查 / 补齐什么 |
|---|---|---|---|---|
| 10 | 🟡 | 身份证密文与密钥 | `sys_config.hr.idcard.key` 存在；员工表里是 AES 密文 | 任务 **1749**（dev-20260914-002）定三档：原样带 / 占位 / 环境变量注入 |
| 11 | 🟡 | 其他凭据 | `sys_config.sms_api_key` 存在（smtp 键名待核）；`jjx-docs/accounts/` 下有 `accounts.json` + `index.html` | 生产不留明文凭据；`accounts/` 不该随仓库进生产 |
| 12 | 🔴 | 数据库账号 | `root/123456`（开发库） | 生产换强口令 + 最小权限账号（迁移账号/应用账号/只读账号分开） |

## 四、环境 / 部署

| # | 层 | 项 | 现状 | 要核查 / 补齐什么 |
|---|---|---|---|---|
| 13 | 🔴 | 生产配置 profile | 只有 `application.yml`（`spring.profiles.active: dev`）+ `application-dev.yml`，**没有 application-prod.yml** | 建 prod profile：库地址、Redis、上传根、日志、开关；确认启动参数 |
| 14 | 🔴 | 上传目录 | `upload/` 50M（仓库根）+ `jjx-server/upload/` 41M **两份**；配置是 `${JJX_UPLOAD_ROOT:/home/administrator/jjx/upload}` | 生产定唯一绝对路径 + `JJX_UPLOAD_ROOT`；确认历史文件在哪一份、按相对路径读文件的功能（附件/图纸/打印模板）都指向它 |
| 15 | 🟡 | 外部依赖 | MySQL、Redis、OCR 服务（`127.0.0.1:8866`） | 启动方式/守护/开机自启/健康检查；`guides/paddleocr-project-isolation-20260912.md` |
| 16 | 🟡 | 网络与证书 | 8080 后端、前端 3000/nginx；内网 HTTPS 方案已有手册 | 端口/防火墙/证书签发与安装（`guides/internal-https-setup-guide-20260904.md`） |

## 五、权限 / 安全

| # | 层 | 项 | 现状 | 要核查 / 补齐什么 |
|---|---|---|---|---|
| 17 | 🔴 | 权限 fail-open | 权限清单为空或含 `*` 时前端判定"全放行"，组件全渲染、各自打后端 → 后端实时判权 403 | 上生产前改 fail-closed + 登录后实时重算权限（两者要一起上） |
| 18 | 🟡 | 跨模块 403 | 角色按模块一刀切（如采购角色打开采购单，表单里取物料下拉就 403） | 给只读查询开口子（`xxx:query` 或"登录即可"的辅助接口） |
| 19 | 🟡 | 审计留痕 | `00_clean_test_data.sql` 会清空 `sys_oper_log`；清理动作库里留不下痕迹 | 生产审计要靠库外：清理/迁移的 stdout 摘要落 `JJX_BACKUP_DIR`（清理脚本已这么做） |

## 六、工程闸门 / 发布纪律

| # | 层 | 项 | 现状 | 要核查 / 补齐什么 |
|---|---|---|---|---|
| 20 | 🔴 | 门禁是否全绿 | 今天 `npm run check:docs` 通过；`check:status-enums`、`vue-tsc --noEmit`、`mvn -o test-compile` 未跑 | 发布前跑 `cd jjx-web && npm run validate` + `cd jjx-server && mvn -o test-compile`，全绿才算 |
| 21 | 🔴 | 工作区收敛 | 现在有大量未提交（并行会话的 java/vue/OCR 改动、被删的迁移、assets、新增迁移 107~109） | 发布前把该提交的提交、该丢弃的丢弃；发布分支工作区必须干净 |
| 22 | 🟡 | 钩子 | `core.hooksPath=scripts/hooks` ✓（本 clone 已装） | 生产机/其他 clone 也要 `bash scripts/install-hooks.sh` |
| 23 | 🟡 | 分支与发布 | 当前 `dev` 分支 | 定：生产分支或 tag、谁发布、发布步骤、谁回滚 |

## 七、上线演练与回滚

| # | 层 | 项 | 要核查 / 补齐什么 |
|---|---|---|---|
| 24 | 🔴 | 空环境演练 | 用"DDL 基线 + 子集快照"在一台干净机器上建库 → 起服务 → 冒烟，走一遍才算真能上 |
| 25 | 🔴 | 回滚方案 | 上线前全库备份（记 md5/表数）+ 回滚步骤 + 数据补丁的回滚脚本（迁移是按号递增的，回滚要写明） |
| 26 | 🔴 | 冒烟清单 | 登录 / 菜单与权限 / 一单到底（销售→生产→入库）/ 打印 / 附件与图纸 / 手机端扫码 |

### 首轮核查结论（2026-09-14，全部只读取证）

- **102~106 实体都执行过**（102：两表已不存在=0；103：5 张新表在=5；104：权限节点在 371/372/373；105：5 个审批列都在）→ 只是**没进版本集合**，补记录即可（`db-migrate.sh --record 102..106`）。
- **105/106 当时想登记的任务码被并发会话占了**（`dev-20260912-015` 实际是"销售询价/样品单页面收尾"、`dev-20260912-016` 实际是"sql 目录精简"）→ 这正是 CONVENTIONS §11 规则 1「任务即锁」要解决的。
- **DDL 基座结论**：HEAD 的 `migrations/` 里只有 `103` 含 5 个 CREATE TABLE，其余建表语句不在仓库；git 里的全库 dump（1908）只有 106 张表且**不含 103 的新表** → **唯一可行路径：从现库生成 DDL 基线（`mysqldump --no-data`）+ 107~109 迁移 + 数据子集快照**。
- **权限 fail-open 三处位置**：`jjx-web/src/permission.ts:51`（`setPermissions(['*'])` 兜底）、`jjx-web/src/store/modules/user.ts:120`、`jjx-web/src/directives/index.ts:45`（`hasPermi()`）。
- **工作区待收敛 56 项**：37 M + 9 D + 10 ??，主体是并行会话的 Java/Vue 改动（工艺资源、档案 OCR、销售样品）、用户删掉的迁移 101~106、3 个 assets 删除。
- **上传目录两份**子目录结构相同（backup/engineering-archive/inquiry/order/product…），50M vs 41M，需定唯一路径。

## 核查进度表（每查一项填一行，没证据不算查过）

| # | 项 | 层 | 状态 | 证据（命令 + 输出 / 提交号） | 日期 |
|---|---|---|---|---|---|
| 1 | 版本记账补 102~106 | 🔴 | 🟨 已查清，待补记录 | 102 两表不存在(0)、103 五表在(5)、104 节点在(371/372/373)、105 五列在 → 待跑 `db-migrate.sh --record 102..106` | 2026-09-14 |
| 2 | 定 DDL 基座方案 | 🔴 | 🟨 已查清，方案已定 | HEAD migrations 仅 103 含 5 个 CREATE TABLE；现库 111 表；dump(1908) 仅 106 表且无 103 新表 → 从现库生成 DDL 基线 | 2026-09-14 |
| 3 | 101~106 归属 | 🟡 | ⬜ 待拍板 | 工作区 ` D` ×6（未提交），内容仍在 git 历史 `7643353f` | 2026-09-14 |
| 4 | 提交 107~109 | 🟡 | ⬜ 待提交 | 三份仍是 `??`；库已应用并记账（version=109） | 2026-09-14 |
| 5 | 生产库连接/断言 | 🔴 | 🟨 已核，待替换 | 脚本/代码均开发口径：库名 `jjx_erp_db`、`root/123456`、备份目录默认仓库同级；dev.yml 用 `${DB_PASSWORD:123456}` 兜底 | 2026-09-14 |
| 6 | 快照重校 | 🔴 | ✅ 已核 | `db-export-init-subset.sh --verify` → ✓ 最新（19 表 / 7336 行）；**107~109 之后需重跑** | 2026-09-14 |
| 7 | DECISIONS 5 项拍板 | 🟡 | ⬜ 未查 | | |
| 8 | 数据缺口对齐 | 🟡 | ⬜ 未查 | | |
| 9 | 生产初始化路径确认 | 🔴 | ⬜ 未查 | | |
| 10 | 脱敏口径（任务 1749） | 🟡 | ⬜ 未查 | | |
| 11 | 凭据清理 | 🟡 | ⬜ 未查 | `jjx-docs/accounts/accounts.json` + `index.html` 仍在仓库；`sys_config.sms_api_key` 存在 | 2026-09-14 |
| 12 | 生产库账号 | 🔴 | ⬜ 未查 | | |
| 13 | application-prod.yml | 🔴 | 🟨 已核，确认缺失 | 只有 `application.yml`（active: dev）+ `application-dev.yml` | 2026-09-14 |
| 14 | 上传目录定唯一路径 | 🔴 | 🟨 已核，待定 | 仓库根 `upload/` 50M 与 `jjx-server/upload/` 41M，子目录结构相同；配置 `${JJX_UPLOAD_ROOT:/home/administrator/jjx/upload}` | 2026-09-14 |
| 15 | 外部依赖启动方式 | 🟡 | ⬜ 未查 | dev.yml: Redis `localhost:6379`、mail `localhost`；OCR `127.0.0.1:8866` | 2026-09-14 |
| 16 | 网络与证书 | 🟡 | ⬜ 未查 | | |
| 17 | 权限 fail-closed | 🔴 | 🟨 已核，位置已定 | `permission.ts:51` `setPermissions(['*'])`；`store/modules/user.ts:120`；`directives/index.ts:45` | 2026-09-14 |
| 18 | 跨模块 403 | 🟡 | ⬜ 未查 | | |
| 19 | 审计留痕 | 🟡 | ⬜ 未查 | | |
| 20 | 门禁全绿 | 🔴 | 🟨 部分 | `check:docs` ✅ 通过；`check:status-enums` / `vue-tsc --noEmit` / `mvn -o test-compile` 未跑 | 2026-09-14 |
| 21 | 工作区收敛 | 🔴 | 🟨 已核，待收敛 | 未提交 56 项：37 M + 9 D + 10 ?? | 2026-09-14 |
| 22 | 钩子安装 | 🟡 | ✅ 已核（本 clone） | `git config --get core.hooksPath` → `scripts/hooks` | 2026-09-14 |
| 23 | 分支与发布方式 | 🟡 | ⬜ 未查 | | |
| 24 | 空环境演练 | 🔴 | ⬜ 未查 | | |
| 25 | 回滚方案 | 🔴 | ⬜ 未查 | | |
| 26 | 冒烟清单 | 🔴 | ⬜ 未查 | | |
