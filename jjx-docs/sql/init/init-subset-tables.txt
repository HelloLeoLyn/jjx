# 数据初始化子集 —— 导出清单（唯一真源）
#
# 用途：scripts/db-export-init-subset.sh 按本清单导出「进入初始化脚本的正式数据」，
#       用于给新环境开账（不是全库备份；全库备份走 CONVENTIONS §2 的 db-migrate.sh / 例行 dump）。
# 判定依据：jjx-docs/sql/init/DECISIONS.md（用户按模块逐条拍板：正式数据进、测试数据不进）。
# 维护规则：
#   1. 增删一行 = 用户拍板后由 Hermes 更新本文件；导出脚本只读不写本文件。
#   2. 表被 DROP / 下线（如迁移 102 的 jjx_screen_master、production_tooling）必须同步移出，
#      否则导出脚本会因"清单里的表不存在"直接中止（防把已废表带进新环境）。
#   3. 空表（0 行的新表）不进数据清单，结构随 DDL 迁移脚本走。
# 变更记录：
#   2026-09-14 首版（任务 dev-20260914-001）：取 09-12 快照的 20 表，移除 jjx_screen_master
#              （2026-09-12 21:43 迁移 102 已 DROP，011 拍板不迁移旧数据）
#              → 19 表。engineering_screen_frame / engineering_screen_plate / engineering_die
#              当前 0 行，不进数据清单。
sys_menu
sys_role
sys_role_menu
sys_dict
sys_dict_item
sys_config
sys_user
sys_user_role
sys_dept
sys_event_config
sys_tag
engineering_standard_process
purchase_supplier
sys_tag_rel
inventory_material
inventory_item
inventory_warehouse
quality_template_registry
hr_employee
