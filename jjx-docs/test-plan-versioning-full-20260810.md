# 打样转标准版本化全链路 验收测试计划（2026-08-10）

> 版本：v1.0（2026-08-10 大黄生成，覆盖当日全部方案改动）
> 范围：资料转移两入口 / 工艺路线（下标+版本号）/ 打样工作台优化 / BOM树形 / 应用料实际投料 / BOM打印预览
> 前置：8080 最新 jar 已重启（含 V20260810_001~003 迁移）；前端 vite 3000 热更新
> 用例编号：TC-520~544（工作台已有 TC-1~156、300~310、400~450、500~515）
> 执行人：HelloLeoLyn（人工），大黄可 Playwright 接管

---

## 一、测试环境准备

| 项 | 值 |
|---|---|
| 后端 | http://localhost:8080（最新 jar，已重启） |
| 前端 | http://localhost:3000（vite 热更新） |
| 数据库 | MySQL jjx_erp_db（root/123456） |
| 登录 | admin / 123456 |

## 二、测试数据准备

- 标准工序「下线跳」（SP-311, process_id=67）已标记 has_index=1；建议另设 1-2 个带下标工序
- 已转标准样品单 ≥1 个（sample_status=7，含工序+材料），供历史复制/常用物料统计
- 板材物料若干（material_type='R'，如 PC 板），非板材物料若干
- 产品 JST001MEOO（product_id=2）已有工艺路线 V2.0

## 三、测试用例

---

## 模块1：资料转移

### [TC-520] 轻量版弹窗：自动匹配 + 手动调整 + 确认转移
- **前置**：样品单=已确认(6)或已转量产(7)，含打样工序/材料
- **步骤**：
  1. 样品单列表 → 点「资料转移」→ 打开轻量版弹窗
  2. 核对自动匹配推荐（工序/物料带出标准项）
  3. 手动改选某工序/物料为其他标准项
  4. 点「确认转移」
- **预期**：
  - 弹窗工序/物料映射带匹配推荐，未匹配项标红
  - 改选后 allMatched 才可确认
  - 确认成功提示 TF-xxx + 版本号；sales_sample_transfer 落库；生成 BOM/工艺路线草稿（approve_status=1）
- **实际结果**：

### [TC-521] 对照版全屏页：增删改/替换/排序/确认转移
- **前置**：样品单已确认，含工序
- **步骤**：
  1. 轻量版弹窗 → 「进入标准编辑」→ 对照版全屏页
  2. 工序：新增/删除/替换标准工序；组合整体拖拽排序
  3. 物料：新增/删除/改用量
  4. 点「确认转移」
- **预期**：对照版左=打样数据只读、右=标准数据可编辑；组合整体拖动（提示"组合工序保持整体"）；确认转移成功
- **实际结果**：

### [TC-522] 轻量版 ↔ 对照版切换数据不丢失
- **前置**：已打开轻量版弹窗
- **步骤**：
  1. 轻量版改选 1 个工序 + 1 个物料
  2. 「进入标准编辑」→ 对照版核对修改仍在
  3. 「返回轻量版」→ 弹窗自动打开，修改仍在
- **预期**：共享 store，两页切换编辑状态不丢失
- **实际结果**：

### [TC-523] 对照版：带下标工序显示红底数字
- **前置**：打样工序含「下线跳」（has_index=1，process_order=4）
- **步骤**：样品单 → 资料转移 → 进入标准编辑，看工序行
- **预期**：「下线跳」行显示 IconStepBadge 图标+红底 ④；普通工序只显示名称
- **实际结果**：

### [TC-524] 资料转移生成版本化 BOM/路线
- **前置**：TC-520/521 已完成转移
- **步骤**：查库/查列表：新 BOM/工艺路线版本、is_current、source_sample_id、parent
- **预期**：新版本 is_current=1、source_sample_id=样品单ID；旧版本失效；product.current_routing_version/bom_version 同步
- **实际结果**：

---

## 模块2：工艺路线

### [TC-525] 下标工序：has_index 标记 + 拖入弹窗 + 红底数字
- **前置**：标准工序 SP-311 已标记带下标；路线编辑页打开
- **步骤**：
  1. 标准工序页确认「下线跳」带下标列=带下标
  2. 工艺路线编辑 → 拖入「下线跳」→ 弹窗输入 4
  3. 表格行显示红底数字 ④；点图标可改数字
- **预期**：has_index=1 显示 IconStepBadge；index_number=4 落库；详情页同样显示
- **实际结果**：

### [TC-526] 版本号显示
- **前置**：工艺路线详情/编辑页打开
- **步骤**：看页面顶部
- **预期**：显示「当前版本：Vx.x」；打样转移来的显示「来源打样单 #id」；升版过的显示「升版自」
- **实际结果**：

### [TC-527] 保存自动升版
- **前置**：路线编辑页打开（当前 V1.0）
- **步骤**：
  1. 修改工序（增/删/改）
  2. 出现黄色警示条「检测到工序内容变更，保存将升级版本」+ 变更说明输入框
  3. 填说明 → 保存
- **预期**：保存成功提示「已升级为 V2.0（旧版本失效）」；刷新后页面顶部 V2.0；旧版本 is_current=0、新版本 parent 指向旧版本；product.current_routing_version 同步；remark 含变更说明
- **实际结果**：

### [TC-528] 无变更保存不升版
- **前置**：路线编辑页打开
- **步骤**：不改任何内容直接保存
- **预期**：无警示条；保存不升版（版本号不变），原地更新
- **实际结果**：

---

## 模块3：打样工作台

### [TC-529] 从历史打样复制
- **前置**：存在已转标准样品单（含工序计划）；当前工作台打开
- **步骤**：顶部「📋 从历史打样复制」→ 选历史单 → 复制到当前
- **预期**：弹窗列出已转标准单（排除当前）；复制后卡片追加到现有卡片后（不覆盖）；保存后生效
- **实际结果**：

### [TC-530] 批量编辑模式
- **前置**：工作台有 ≥2 张卡片
- **步骤**：
  1. 点「批量编辑」→ 卡片出现复选框
  2. 勾选多张 → 批量栏出现
  3. 统一设置工序类别 / 批量删除 / 批量添加材料
- **预期**：勾选卡片高亮；统一类别应用到所有选中；批量删除确认后移除；批量加材料加到所有选中卡片
- **实际结果**：

### [TC-531] 常用物料快捷区
- **前置**：历史打样数据存在（同客户优先）
- **步骤**：工作台工序计划区上方查看「⭐ 常用物料」标签 → 点击某个物料
- **预期**：显示历史高频 Top10（带使用次数）；点击后物料加入当前卡片材料列表
- **实际结果**：

### [TC-532] 保存状态标记
- **前置**：工作台有卡片
- **步骤**：
  1. 修改卡片内容（拖入工序/改材料/改描述）
  2. 观察状态 → 点保存 → 观察变化
- **预期**：修改后变「⏳ 未同步」；保存中「🔄」；成功「✅ 已同步」；失败「❌」（可断网模拟）
- **实际结果**：

### [TC-533] 未同步离开拦截
- **前置**：卡片处于未同步状态
- **步骤**：点「返回打样平台」或切换路由
- **预期**：弹窗「有卡片尚未同步保存，确定离开吗？」；取消留下，确认离开
- **实际结果**：

---

## 模块4：BOM 树形展示

### [TC-534] 树形展示与展开折叠
- **前置**：BOM 已有父子层级数据（或按 TC-535 建立后）
- **步骤**：BOM 编辑页查看明细表格
- **预期**：树形表格展示（子物料缩进）；展开/折叠正常；默认全部展开
- **实际结果**：

### [TC-535] 添加子物料
- **前置**：BOM 编辑页打开
- **步骤**：根物料行点「子物料」→ 新行挂到该节点下 → 填物料 → 保存 → 重开
- **预期**：子物料缩进在父节点下；保存后重开层级保留（parent_material_id 落库正确）
- **实际结果**：

### [TC-536] 删除保护
- **前置**：父节点下有子物料
- **步骤**：点父节点删除
- **预期**：提示「请先删除子节点，再删除该物料」，阻止删除；删完子节点后父节点可删
- **实际结果**：

### [TC-537] 保存层级落库
- **前置**：BOM 有父子层级
- **步骤**：保存 → 查库 engineering_bom_item.parent_material_id
- **预期**：子节点 parent_material_id=父节点新 item_id（更新后旧 ID 映射正确）；查询接口返回 parent 关系
- **实际结果**：

### [TC-538] Excel 导入全部为根节点
- **前置**：准备含 2 行物料的 Excel
- **步骤**：BOM 编辑页导入 → 看明细
- **预期**：导入成功提示「全部为根节点」；所有行无层级（根节点）；用「子物料」手动建层级
- **实际结果**：

---

## 模块5：应用料/实际投料

### [TC-539] 应用料计算
- **前置**：BOM 编辑页，物料含用量/损耗率
- **步骤**：设用量=5、损耗率=10% → 看「应用料」列
- **预期**：应用料=5×1.1=5.5（前端实时预览）；保存后落库 applied_qty=5.5000；查询回显
- **实际结果**：

### [TC-540] 实际投料计算（板材向上取整）
- **前置**：板材物料（material_type='R'）最低投料=10
- **步骤**：用量=5、损耗=10%（应用料=5.5）→ 看「实际投料」列
- **预期**：实际投料=CEIL(5.5/10)×10=10；非板材或最低投料=0 → 实际投料=应用料；R型显示 ⓘ 提示
- **实际结果**：

### [TC-541] Excel 导入直读应用料/实际投料
- **前置**：Excel 含「应用料」「实际投料」列
- **步骤**：导入 → 保存 → 查库
- **预期**：有值的列直读落库（不重算）；无值列后端自动计算
- **实际结果**：

---

## 模块6：BOM 打印预览

### [TC-542] 打印预览弹窗
- **前置**：BOM 编辑页有明细
- **步骤**：footer 点「打印预览」
- **预期**：弹窗显示作业指导书：左栏物料清单（序号/项目/材料/规格及模数/用量/单位）+ 右栏作业流程（面板/上线/下线分组）；产品/BOM 版本信息在标题区
- **实际结果**：

### [TC-543] 打印功能
- **前置**：TC-542 弹窗打开
- **步骤**：点「🖨 打印」
- **预期**：浏览器打印对话框；打印预览只含指导书内容（无弹窗外壳/按钮）；输出纸质版可读
- **实际结果**：

---

## 四、冒烟测试用例

### [TC-544] 全链路冒烟
- **前置**：测试数据就绪
- **步骤**（10 分钟快速走查）：
  1. 资料转移轻量版打开→进入对照版→带下标工序显示④
  2. 工艺路线编辑：拖入带下标工序→输数字→显示红底数字→改内容保存→自动升版
  3. 打样工作台：批量编辑勾选→批量删除；常用物料点击加入
  4. BOM：添加根物料→子物料→保存→重开树形保留；应用料/实际投料自动算；打印预览弹窗
- **预期**：各环节主路径无报错，关键数据落库正确
- **实际结果**：

---

## 五、回归测试范围（本次改动可能影响）

| 模块 | 回归点 |
|---|---|
| 标准工序 | 列表/新增/编辑/启用停用（has_index 字段不破坏原有 CRUD） |
| 工艺路线 | 原有组合拖拽/工时汇总/备注；复制新版本接口 |
| 样品单 | 转量产（convert-check 就绪检查）；资料转移旧入口 |
| BOM | 原平铺数据的展示兼容（旧数据无 parent_material_id=根）；BOM 审批流 |
| 打印 | 报价单/调拨/出入库等既有打印页（@media print 未动） |

## 六、注意

- 测试数据用后清理（参考 backups/ 最新备份可恢复）
- 失败点在工作台「➕登记任务」→ sys_task(dev)
- BOM 应用料/实际投料功能有独立标记任务 DEV-763（效果不满意可 revert commit 88eff5b）
