node /mnt/d/openclaw-workspace/docs/task-dashboard/server.js

# 先杀掉旧进程
lsof -ti:8899 | xargs -r kill -9
# 再后台启动
nohup node /mnt/d/openclaw-workspace/docs/task-dashboard/server.js > /dev/null 2>&1 &



好。你在 VS Code 终端跑这几步就行：

1. 先杀掉旧后端

复制
lsof -ti:8080 | xargs kill -9
2. 重新打包（加载最新代码）

复制
cd /mnt/d/workspace/jjx/jjx-server && mvn package -DskipTests
3. 启动后端

复制
nohup java -jar target/jjx-server-1.0.0.jar --server.port=8080 > /tmp/jjx.log 2>&1 &
4. 启动前端

复制
cd /mnt/d/workspace/jjx/jjx-web && npx vite --host 0.0.0.0
跑起来后访问 http://localhost:5174 登录就能测了。账号 admin，密码你看后端配置。

有什么问题告诉我。

temp = "$2a$10$Qx.VUAClCS1.cVI5MvXY9eVDun69Mh28xG0Y46Ni1iEA.RIQjFUN6",
inputPassword = "123456"