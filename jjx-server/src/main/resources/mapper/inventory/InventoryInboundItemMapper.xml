<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jjx.inventory.mapper.InventoryInboundItemMapper">

    <!-- 批量插入入库单明细 -->
    <insert id="batchInsert" parameterType="list">
        INSERT INTO inventory_inbound_item (
            inbound_id, material_id, material_code, material_name,
            specification, unit, quantity, unit_price, amount,
            batch_no, production_date, expiry_date, location_id,
            qualified_quantity, rejected_quantity, reject_reason,
            sort_order, remark
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.inboundId}, #{item.materialId}, #{item.materialCode}, #{item.materialName},
                #{item.specification}, #{item.unit}, #{item.quantity}, #{item.unitPrice}, #{item.amount},
                #{item.batchNo}, #{item.productionDate}, #{item.expiryDate}, #{item.locationId},
                #{item.qualifiedQuantity}, #{item.rejectedQuantity}, #{item.rejectReason},
                #{item.sortOrder}, #{item.remark}
            )
        </foreach>
    </insert>

</mapper>
